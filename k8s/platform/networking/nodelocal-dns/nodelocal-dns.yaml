apiVersion: v1
kind: ConfigMap
metadata:
  name: nodelocal-dns-runtime-baseline
  namespace: prod-system
  labels:
    app.kubernetes.io/name: nodelocal-dns
    app.kubernetes.io/part-of: bizkit
    app.kubernetes.io/component: dns
data:
  local_ip: "169.254.20.10"
  kube_dns_service_ip: "10.96.0.10"
  request_cpu: "50m"
  request_memory: "64Mi"
  limit_cpu: "200m"
  limit_memory: "128Mi"
  notes: |
    NodeLocal DNSCache는 DaemonSet 기준으로 모든 worker node에 1개씩 배치한다.
    실제 daemonset bundle은 kubeadm/kube-proxy/coredns 설치 방식과 함께 반입해야 하므로,
    현재 파일은 런타임 baseline을 고정하는 목적이다.
