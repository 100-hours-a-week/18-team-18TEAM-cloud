apiVersion: v1
kind: Service
metadata:
  name: ai-model
  namespace: prod-ai
  labels:
    app.kubernetes.io/name: ai-model
    app.kubernetes.io/part-of: bizkit
    app.kubernetes.io/component: ai-model
    environment: prod
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-model
    app.kubernetes.io/part-of: bizkit
    environment: prod
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
