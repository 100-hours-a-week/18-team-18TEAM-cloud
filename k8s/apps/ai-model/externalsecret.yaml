apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ai-model-env
  labels:
    app.kubernetes.io/name: ai-model
    app.kubernetes.io/part-of: bizkit
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager
  target:
    name: ai-model-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: TODO_AI_MODEL_SECRET_KEY # TODO(B): 환경별 외부 시크릿 경로로 교체
