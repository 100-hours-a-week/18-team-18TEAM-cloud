apiVersion: v1
kind: Service
metadata:
  name: be
  labels:
    app.kubernetes.io/name: be
    app.kubernetes.io/part-of: bizkit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: be
  ports:
    - name: http
      port: 8080
      targetPort: http
