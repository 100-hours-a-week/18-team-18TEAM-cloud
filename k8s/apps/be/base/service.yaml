apiVersion: v1
kind: Service
metadata:
  name: be
  namespace: prod-be
  labels:
    app.kubernetes.io/name: be
    app.kubernetes.io/part-of: bizkit
    app.kubernetes.io/component: backend
    environment: prod
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: be
    app.kubernetes.io/part-of: bizkit
    environment: prod
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
